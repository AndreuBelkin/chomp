import React, { FC } from "react";
import "./Header.css";

const Header: FC = () => {
  return (
    <header className='header container'>
      <p>Logo</p>
      <p>Navigation</p>
    </header>
  );
};

export default Header;
